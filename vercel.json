{
  "version": 2,
  "builds": [{ "src": "public/index.js", "use": "@vercel/node" }],
  "routes": [
    { "src": "/(.*)", "dest": "/graphql" },
    { "src": "/graphql/(.*)", "dest": "/graphql" }
  ]
}
